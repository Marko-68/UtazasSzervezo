<UserControl x:Class="UtazasSzervezo_Admin.Views.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:vm="clr-namespace:UtazasSzervezo_Admin.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    
    <UserControl.DataContext>
        <vm:StatisticsViewModel/>
    </UserControl.DataContext>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Havi foglalások száma -->
        <GroupBox Header="Havi foglalások" Grid.Row="0" Margin="0 0 0 10">
            <lvc:CartesianChart Series="{Binding BookingsPerMonthSeries}" LegendLocation="Right">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Title="Hónapok" Labels="{Binding BookingsPerMonthLabels}"/>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis Title="Foglalások száma"/>
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>
        </GroupBox>

        <!-- Havi bevétel -->
        <GroupBox Header="Havi bevétel" Grid.Row="1" Margin="0 0 0 10">
            <lvc:CartesianChart Series="{Binding RevenuePerMonthSeries}" LegendLocation="Right">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Title="Hónapok" Labels="{Binding RevenuePerMonthLabels}"/>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis Title="Bevétel (Ft)" LabelFormatter="{Binding CurrencyFormatter}"/>
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>
        </GroupBox>

        <!-- Legnépszerűbb városok -->
        <GroupBox Header="Legnépszerűbb városok" Grid.Row="2">
            <lvc:PieChart Series="{Binding TopCitiesSeries}" LegendLocation="Bottom"/>
        </GroupBox>
    </Grid>
    </UserControl>

