<Window x:Class="UtazasSzervezo_Admin.Views.AccommodationEditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Szállás szerkesztése" Height="500" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0 0 0 10">
            <TextBlock Text="Név:"/>
            <TextBox Text="{Binding name}" Margin="0 0 0 10"/>

            <TextBlock Text="Leírás:"/>
            <TextBox Text="{Binding description}" Margin="0 0 0 10" AcceptsReturn="True" Height="60"/>

            <TextBlock Text="Típus:"/>
            <ComboBox SelectedItem="{Binding type}" Margin="0 0 0 10">
                <ComboBoxItem Content="Hotel"/>
                <ComboBoxItem Content="Apartman"/>
                <ComboBoxItem Content="Hostel"/>
                <ComboBoxItem Content="Egyéb"/>
            </ComboBox>

            <TextBlock Text="Szobák száma:"/>
            <TextBox Text="{Binding number_of_rooms}" Margin="0 0 0 10"/>

            <TextBlock Text="Max. személy:"/>
            <TextBox Text="{Binding max_person}" Margin="0 0 0 10"/>

            <TextBlock Text="Cím:"/>
            <TextBox Text="{Binding address}" Margin="0 0 0 10"/>

            <TextBlock Text="Város:"/>
            <TextBox Text="{Binding city}" Margin="0 0 0 10"/>

            <TextBlock Text="Ország:"/>
            <TextBox Text="{Binding country}" Margin="0 0 0 10"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Mégse" Click="Cancel_Click" Margin="0 0 10 0"/>
            <Button Content="Mentés" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>