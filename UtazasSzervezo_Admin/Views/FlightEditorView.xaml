<Window x:Class="UtazasSzervezo_Admin.Views.FlightEditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Járat szerkesztése" Height="400" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0 0 0 10">
            <TextBlock Text="Légitársaság:"/>
            <TextBox Text="{Binding airline}" Margin="0 0 0 10"/>

            <TextBlock Text="Indulás ideje:"/>
            <DatePicker SelectedDate="{Binding departure_time}" Margin="0 0 0 10"/>

            <TextBlock Text="Érkezés ideje:"/>
            <DatePicker SelectedDate="{Binding arrival_time}" Margin="0 0 0 10"/>

            <TextBlock Text="Indulási reptér (IATA kód):"/>
            <TextBox Text="{Binding departure_airport}" Margin="0 0 0 10"/>

            <TextBlock Text="Érkezési reptér (IATA kód):"/>
            <TextBox Text="{Binding destination_airport}" Margin="0 0 0 10"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Mégse" Click="Cancel_Click" Margin="0 0 10 0"/>
            <Button Content="Mentés" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
